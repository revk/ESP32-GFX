#ifdef	CONFIG_GFX_BUILD_SUFFIX_EPD75K
bit	epd.slow	0		// Use slow (default) LUT for full refresh
bit	epd.sleep	0		// Use deep sleep
bit	epd.amv		1		// Send AMV (VCOM cal, slower start)
bit	epd.pfs		1		// Send PFS (Power off 3)
bit	epd.evs		0		// Send EVS (Env voltage float)
s8	epd.tse		0	.set	// TSE Temperature adjust, -8 to +7
#endif
